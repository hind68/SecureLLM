# Database

PostgreSQL and database migrations will be added later, after the first backend/frontend flow works.

The database will eventually store users, roles, audit logs, request history, and related gateway metadata.

